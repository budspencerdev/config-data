server:
  port: 8088

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: micro01
          uri: http://MICRO01/
          predicates:
            - Path=/afiliado/**
        - id: micro02
          uri: http://localhost:8086/
          predicates:
            - Path=/afp/**
        - id: micro03
          uri: http://localhost:8087/
          predicates:
            - Path=/poliza/**
